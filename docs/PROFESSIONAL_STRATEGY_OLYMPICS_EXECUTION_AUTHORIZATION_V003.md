# Professional Strategy Olympics execution authorization V003

> PROSPECTIVE CONTRACT ONLY — NO AUTHORIZATION EXISTS AND NO TRIAL RAN

This milestone closes the identity and reuse gaps discovered at the inaugural
execution gate. It changes no strategy, executor, simulator, lifecycle, scoring,
V001 orchestrator, V002 adapter, V003 input adapter, or canonical manifest.

The contract freezes one authoritative outer `lineage_run_identity`. It binds the
PR #21 merge, V001 through V004 identities, the canonical fixture and manifest,
the exact projected V001 input identity, the subordinate V001 run identity, the
permitted synthetic operation, and a maximum execution count of one. The
subordinate V001 identity remains visible; it is never misrepresented as the
complete lineage identity.

A future human-approved authorization must use the exact V003 schema and fields,
carry all access prohibitions as true, and bind the implementation identity of
this adapter. Validation deterministically projects that outer artifact into the
unchanged exact-field V001 authorization schema and invokes the frozen V001 gate.

Consumption is claimed atomically by authorization identity before any future
artifact generation. The claim persists on failure, records execution count one,
and causes every later claim to fail as already consumed. Test-only fixtures
prove projection and reuse rejection; they are not official authorizations.

The contract itself sets `authorization_creation_permitted`,
`trial_execution_permitted`, and `publication_permitted` to false. There is no
official authorization artifact, no result bundle, no ranking, and execution
count remains zero. A separately reviewed milestone must create the one-use
artifact and invoke a future execution wrapper that consumes before delegating to
the unchanged V001 artifact builder.
